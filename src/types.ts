export interface TechnologyType {
  id: string;
  name: string;
  category: string;
  description: string;
  icon: string;
  badge?: string;
  badgeColor?: string;
  rating: number;
  difficulty: string;
  docsUrl?: string;
}
